rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ログインしているユーザーであれば、すべてのドキュメントの読み書きを許可します。
      // これにより、有効期限の問題が解決し、認証されたユーザーがアプリを利用できるようになります。
      allow read, write: if request.auth != null;
    }
  }
}